{
	"name": "p-request-retry",
	"version": "1.0.0",
	"description": "Retry a promise-returning or async function",
	"license": "MIT",
	"type": "module",
	"exports": {
		".": {
		  "import": "./dist/index.mjs",
		  "require": "./dist/index.cjs"
		}
	},
	"engines": {
		"node": "^12.20.0 || ^14.13.1 || >=16.0.0"
	},
	"scripts": {
		"test": "xo && ava && tsd",
		"build": "npx unbuild"
	},
	"files": [
		"index.js",
		"index.d.ts"
	],
	"keywords": [
		"promise",
		"retry",
		"retries",
		"operation",
		"failed",
		"rejected",
		"try",
		"exponential",
		"backoff",
		"attempt",
		"async",
		"await",
		"promises",
		"concurrently",
		"concurrency",
		"parallel",
		"bluebird"
	],
	"dependencies": {
		"@types/retry": "0.12.1",
		"retry": "^0.13.1"
	},
	"devDependencies": {
		"ava": "^4.1.0",
		"delay": "^5.0.0",
		"tsd": "^0.19.1",
		"xo": "^0.48.0",
		"unbuild": "0.8.10"
	}
}
